export const metadata = {
    title: "Montagab - About",
  };
  export default function AboutLayout({
    children,
  }: Readonly<{
    children: React.ReactNode;
  }>) {
    return (
      children
    );
  }
  